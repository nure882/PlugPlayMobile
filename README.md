# PlugPlay Mobile

**PlugPlay Mobile** — це Android-додаток для інтернет-магазину, побудований на базі Jetpack Compose та принципах чистої архітектури (Clean Architecture).

## 🛠 Стек технологій

* **Мова**: Kotlin
* **UI**: Jetpack Compose для декларативного інтерфейсу
* **Архітектура**: MVVM + Clean Architecture (Data, Domain, Presentation)
* **DI**: Hilt (Dagger) для впровадження залежностей
* **Мережа**: Retrofit 2 та OkHttp 3
* **Асинхронність**: Kotlin Coroutines & Flow
* **Зображення**: Coil
* **Платежі**: Інтеграція з LiqPay SDK

## 📂 Структура проекту

* **`data/`**: Реалізація репозиторіїв, API (Retrofit), DTO моделі та локальні джерела даних (SharedPreferences).
* **`domain/`**: Бізнес-логіка, включаючи UseCases, інтерфейси репозиторіїв та базові моделі даних.
* **`presentation/`**: UI-шар, що містить Compose-екрани та ViewModels для управління станом.
* **`di/`**: Модулі Hilt для налаштування мережевого шару та репозиторіїв.
* **`util/`**: Допоміжні класи для роботи з платежами (LiqPay) та локалізацією.

## ✨ Основні можливості

* **Каталог та пошук**: Перегляд товарів, фільтрація за категоріями та пошук за назвою.
* **Кошик**: Управління списком покупок з локальним збереженням стану.
* **Авторизація**: Вхід за логіном/паролем та через Google Sign-In.
* **Замовлення**: Оформлення замовлення з вибором адреси та методу доставки.
* **Оплата**: Безпечні платежі через шлюз LiqPay.

## 🚀 Розгортання та запуск

### Налаштування середовища
1. Встановіть **Android Studio** (рекомендовано останню стабільну версію).
2. Переконайтеся, що у вас встановлено **JDK 17** або вище.
3. Налаштуйте `baseUrl` у файлі `plugplaymobile/di/module/NetworkModule.kt`, щоб додаток міг з'єднатися з вашим бекендом.

### Запуск Debug-версії
1. Підключіть фізичний пристрій або запустіть емулятор (API 26+).
2. Натисніть кнопку **Run** в Android Studio.

### Збірка та запуск релізу (Release)
Для створення фінальної версії додатка:
1. Перейдіть до **Build > Generate Signed Bundle / APK...**
2. Виберіть **APK** або **Android App Bundle**.
3. Створіть новий або виберіть існуючий **KeyStore** (ключ підпису).
4. Виберіть build variant: **release**.
5. Після завершення збірки, APK файл буде доступний у папці `app/release/`.

### Завантаження зібраного застосунку
Для завантаження останньої зібраної версії:
1. Перейдіть до **Вкладинки "Releases"**
2. Оберіть **.apk** файл, що відповідає архітектурі вашого пристрою

**Важливо:** Для роботи LiqPay у релізній збірці переконайтеся, що на бекенді вірно налаштовані ключі (PublicKey/PrivateKey).

## 💳 Інтеграція платежів

При оформленні замовлення додаток надсилає дані на сервер, який повертає зашифрований пакет (`data` та `signature`) для ініціалізації LiqPay. Процес оплати завершується викликом платіжного шлюзу безпосередньо з інтерфейсу оформлення замовлення.
